/** @format */

import {AppRegistry} from 'react-native';
import VideoPlayScene from './VideoPlayScene';
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => VideoPlayScene);
